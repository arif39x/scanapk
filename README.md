# scanapk 
`scan` apk is a tool for checking apk permissions,every HTTP call, SMS sent, crypto operation, file read/write, process execution, and data access.captures all HTTP/HTTPS traffic the app sends.watches Android system logs for errors, crashes, and API calls matching suspicious patterns
